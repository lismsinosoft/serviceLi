<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<generatorConfiguration>
    <!-- mysql连接配置 -->
    <properties resource="config/mysql-connector-config.properties" />

    <!-- mysql驱动 -->
    <classPathEntry location="${jdbc.driverLocation}" />

    <context id="mysqlTables" targetRuntime="MyBatis3">
        <!-- sqlmap是否合并 -->
        <property name="mergeable" value="false" />

        <commentGenerator>
            <property name="suppressDate" value="true" />
            <property name="suppressAllComments" value="true" />
        </commentGenerator>

        <!--数据库链接地址账号密码 -->
        <jdbcConnection
                driverClass="${jdbc.driverClass}"
                connectionURL="${jdbc.connectionURL}"
                userId="${jdbc.userId}"
                password="${jdbc.password}">
        </jdbcConnection>

        <javaTypeResolver>
            <property name="forceBigDecimals" value="false" />
        </javaTypeResolver>

        <!--生成Model类存放位置 -->
        <javaModelGenerator targetPackage="com.sinosoft.capacity.db.pojo" targetProject="src/main/java">
            <property name="enableSubPackages" value="true" />
            <property name="trimStrings" value="true" />
        </javaModelGenerator>

        <!--生成映射文件存放位置 -->
        <sqlMapGenerator targetPackage="mapper.base" targetProject="src/main/java">
            <property name="enableSubPackages" value="true" />
        </sqlMapGenerator>

        <!--生成Dao类存放位置 -->
        <javaClientGenerator type="XMLMAPPER" targetPackage="com.sinosoft.capacity.db.dao" targetProject="src/main/java">
            <property name="enableSubPackages" value="true" />
        </javaClientGenerator>

        <!--生成对应表及类名 -->
        <table tableName="REQ_REQUIREMENT_RECORD" mapperName="ReqRequirementRecordDAO">
            <columnOverride column="CREATE_DATE" jdbcType="TIMESTAMP" />
            <columnOverride column="IS_DELETE" jdbcType="INTEGER"  />
        </table>
        <table tableName="REQ_COST_RECORD" mapperName="ReqCostRecordDAO">
            <columnOverride column="SERVER_START_TIME" jdbcType="TIMESTAMP" />
            <columnOverride column="SERVER_END_TIME" jdbcType="TIMESTAMP" />
        </table>
        <table tableName="REQ_USER_MANAGE" mapperName="ReqUserManageDAO">
            <columnOverride column="ID" jdbcType="INTEGER" />
            <columnOverride column="CREATE_DATE" jdbcType="TIMESTAMP" />
            <columnOverride column="MODIFY_DATE" jdbcType="TIMESTAMP" />
            <columnOverride column="IS_DELETE" jdbcType="INTEGER" />
        </table>
    </context>
</generatorConfiguration>